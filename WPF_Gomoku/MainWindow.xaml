<Window x:Class="WPF_Gomoku.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF_Gomoku" Height="600" Width="1000"
        xmlns:board="clr-namespace:WPF_Gomoku">
    <Window.Background>
        <RadialGradientBrush>
            <GradientStop Color="#FF7A84F0" Offset="1"/>
            <GradientStop Color="#FF50ECEC" Offset="0.424"/>
        </RadialGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="750*"/>
            <ColumnDefinition Width="250*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Name="maingrid" Margin="10,50,248,33"  SizeChanged="maingrid_SizeChanged">
            <board:Board x:Name="gboard"/>
        </Grid>

        <Grid Grid.Column="1">
            <board:ChatBox Margin="0,50,10,115"/>
        </Grid>
    </Grid>
</Window>
